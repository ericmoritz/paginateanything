from setuptools import setup


setup(name="paginateanything",
      description="""A generic pagination module.  It doesn't care what you're
paginating as long as you have a count and a per_page value""",
      py_modules=["paginateanything"],
      test_suite="tests")
    
